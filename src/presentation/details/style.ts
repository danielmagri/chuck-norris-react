import { styled } from "@mui/material";

export const ErrorLabelStyle = styled("label")({
    display: "flex",
    flex: 1,
    color: 'red',
    justifyContent: "center",
    flexWrap: "wrap",
    borderRadius: 4,
  });