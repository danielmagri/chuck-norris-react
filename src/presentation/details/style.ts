import { styled } from "@mui/material";

export const ChuckGridviewStyle = styled("div")({
    display: "flex",
    flex: 1,
    justifyContent: "center",
    flexWrap: "wrap",
    borderRadius: 4,
  });